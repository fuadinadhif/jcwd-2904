import React from "react";

export const metadata = {
  title: "Detail Collections",
  description: "Monalisa details",
};

export default function SlugLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
